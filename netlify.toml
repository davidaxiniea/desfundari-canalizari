# Netlify — build static Astro
[build]
  command = "npm run build"
  publish = "dist"

# Cache lung pentru asseturile cu hash din build
[[headers]]
  for = "/_astro/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
